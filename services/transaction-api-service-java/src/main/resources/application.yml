server:
  port: 4000

spring:
  datasource:
    url: jdbc:postgresql://txn-db:5432/transaction_processor
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    property-naming-strategy: SNAKE_CASE

app:
  jwt:
    secret: dev-jwt-secret-32-chars-min-length
  jwt-service:
    url: http://jwt-api-service:3000
